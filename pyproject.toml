[project]
name = "fleet-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Zachary BENSALEM", email = "zachary@qredence.ai" }
]
requires-python = ">=3.13,<3.14"
dependencies = [
    "verifiers==0.2.0",
]

[project.scripts]
fleet-agent = "fleet_agent:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
